
spring:
  application:
    name: licensing-service
    profiles:
      active: dev
    config:
      import: configserver:http://localhost:8071

      server:
        port:8090
      logging:
          level:
              web: debug